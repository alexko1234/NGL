
# this is the main configuration file for the application.
# Secret key

ngl.env=PROD

# ~~~~~
# The secret key is used to secure cryptographics functions.
# If you deploy your application to several instances be sure to use the same key!
application.secret="SkBwig:nNLT^Z4InhuW`<2TjM0ZnvqU]64[L?^ptZG_NnhlWTgkLj4e4iNN<TCii"

# The application languages
# ~~~~~
application.langs="fr,en"

# Global object class
# ~~~~~
# Define the Global object class for this application.
# Default to Global in the root package.
# application.global=Global

# Router 
# ~~~~~
# Define the Router object to use for this application.
# This router will be looked up first when the application is starting up,
# so make sure this is the entry point. 
# Furthermore, it's assumed your route file is named properly. 
# So for an application router like `conf/my.application.Router`,
# you may need to define a router file `my.application.routes`.
# Default to Routes in the root package (and `conf/routes`)
# application.router=my.application.Routes

# Database configuration
db.default.driver=com.mysql.jdbc.Driver
db.default.partitionCount=1
db.default.maxConnectionsPerPartition=6
db.default.minConnectionsPerPartition=1
db.default.logStatements=true
db.default.jndiName=ngl
#db.default.url="jdbc:mysql://mysqlcns.genoscope.cns.fr:3307/NGL"
db.default.url="jdbc:mysql://db.cng.fr:3306/NGL"
db.default.user="ngl"
db.default.password="ngl@dmin"


##LIMS CNG
db.lims.driver=org.postgresql.Driver
db.lims.partitionCount=1
db.lims.maxConnectionsPerPartition=6
db.lims.minConnectionsPerPartition=1
db.lims.logStatements=true
db.lims.jndiName=lims
db.lims.url="jdbc:postgresql://db.cng.fr/solexaprod"
db.lims.user=ngl_bi
db.lims.password=N3wG3nLim5


db.tara.driver=com.mysql.jdbc.Driver
db.tara.url="jdbc:mysql://mysqlcns.genoscope.cns.fr:3307/Tara"
db.tara.user=lims_user
db.tara.password="Lims_PassWrd"
db.tara.jndiName=tara
db.tara.partitionCount=1
db.tara.maxConnectionsPerPartition=1
db.tara.minConnectionsPerPartition=0
db.tara.logStatements=true


#
# You can expose this datasource via JNDI if needed (Useful for JPA)


# Evolutions
# ~~~~~
# You can disable evolutions if needed
evolutionplugin=disabled


# Logger
# ~~~~~
# You can also configure logback (http://logback.qos.ch/), by providing a logger.xml file in the conf directory .

# Root logger:
#logger.root=ERROR

# Logger used by the framework:
#logger.play=INFO

# Logger provided to your application:
#logger.application=DEBUG

# Configure the database name
mongodb.defaultWriteConcern="SAFE"
mongodb.database=NGL
mongodb.credentials="ngl:NGL,Pr0d"
mongodb.servers="gaia.cng.fr:27017"


#Asset configuration
asset.url="http://assets.cng.fr"

# Implementation DAO
institute="CNG"

#Active/Desactive import de donnée
import.data=true
# Run import pour un institut
import.institute=CNG

#Rules
rules.key=nglBI
rules.kbasename=ngl-bi-cng
#

# Variables used in logger.xml
mail.smtp.host=smtp.genoscope.cns.fr
import.email.from="ngl-data@cng.fr"
import.email.to="galbini@genoscope.cns.fr"


reporting.active=false
reporting.email.from="ngl-data@cng.fr"
reporting.email.to="galbini@genoscope.cns.fr"
reporting.email.subject="Reporting journalier : contrôle des données de la base Mongo"
